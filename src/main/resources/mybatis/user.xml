<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wanke.com.mapper.UserMapper">
	<insert id="signUp" parameterType="org.wanke.com.entity.User">
        insert into sys_user(
            account,
			password,
			user_name,
			phone,
			sex,
			store_id,
			enable,
			type,
			head_url,
			create_time,
			create_id)
		values (
	        #{account},
	        #{password},
	        #{user_name},
	        #{phone},
			#{sex},
			#{store_id},
			#{enable},
			#{type},
			#{head_url},
			#{create_time},
			#{create_id})
    </insert>
</mapper>